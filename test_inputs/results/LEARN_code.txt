MC	<p>MC</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	correct	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	incorrect
TF	<p>TF</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	true
ESS	<p>Essay</p> <pre><code>def slay()</code></pre> 	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 
MAT	<p>Match</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	<p>d1</p>	<p>e1</p>	<p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 
FIB	<p>FIB one answer ___</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	before code ```python assert slay print("serve") ``` after code
FIB_PLUS	<p>FIB many answers desc [a] [b]</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	a	before code ```python assert slay print("serve") ``` after code		b	before code ```python assert slay print("serve") ``` after code	
NUM	<p>NUM</p> <p>before code</p> <div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="cf">assert</span> slay</span><br><span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="bu">print</span>(<span class="st">&quot;serve&quot;</span>)</span></code></pre></div> <p>after code</p> 	100	10
